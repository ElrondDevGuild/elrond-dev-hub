import RequiresAuth from "../../components/RequiresAuth";

export default function Create() {
    return (
        <RequiresAuth>
            <div>
                Create Bounty
            </div>
        </RequiresAuth>
    );
};